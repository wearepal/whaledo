---
# usage: hydra/launcher=slurm_goedel
defaults:
  - submitit_slurm
  - _self_

partition: kyiv
cpus_per_task: 24  
gpus_per_task: 4 
timeout_min: 99999

# additional_parameters:
#   gres: "gpu"  # 1 GPU
